{% extends 'base.html.twig' %}

{% block body %}
    <h1>Project</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project.description }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ project.status }}</td>
            </tr>
            <tr>
                <th>Opendate</th>
                <td>{% if project.openDate %}{{ project.openDate|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Closedate</th>
                <td>{% if project.closeDate %}{{ project.closeDate|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Creator</th>
                <td>{{ project.creator }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('project_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('project_edit', { 'id': project.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}



{#{% extends 'base.html.twig' %}
 {% set title = "" %}
{% block body %}
<!-- Hover Rows -->
<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header bg-light-blue">
                <h2>{{ project.name }}</h2>
            </div>
            <div class="body">
                {% for message in discussion.messages %}
                {% set current = 0 %}
                {% if message.author.id == app.user.id %}
                    {% set current = 1 %}
                {% endif %}
                <div class="row">
                    {% if current %}
                    <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12">
                        {% else %}
                        <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12 pull-right">
                            {% endif %}
                            <div class="card">
                                <div class="bg-light-blue">
                                    <div class="media">
                                        <div class="media-left">
                                            <img class="media-object" src="{{ asset('images/user.png') }}" width="48" height="48">
                                        </div>
                                        <div class="media-body">
                                            <p class="media-heading">{{ message.author.firstname ~ " " ~ message.author.lastname ~ " le " ~ message.date|date('d/m/Y')  }} </p>
                                        </div>
                                    </div>
                                </div>
                                <div class="body">
                                    <p>{{ message.content }}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endfor %}

                    {{ form_start(form) }}
                    {{ form_label(form.content) }}
                    {{ form_widget(form.content, {'attr': {'class': 'form-control', 'type': 'text'}}) }}
                    <button type="submit">Valider</button>
                    {{ form_end(form) }}


                </div>
            </div>
        </div>
    </div>
    <!-- #END# Hover Rows -->

    {% endblock body %}#}

