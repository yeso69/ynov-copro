{% extends 'base.html.twig' %}

{% set title = 'Messages'|trans %}

{% block body %}
    <div class="card">
        <div class="header bg-pink">
            <h2>Editer {{ message.subject }}</h2>
        </div>

        <div class="body">
            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input type="submit" value="Edit" />
            {{ form_end(edit_form) }}            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('message_archive', {'id':message.id}) }}"><i class="material-icons">archive</i>Archive</a>
            {% endif %}
        </div>
    </div>

{% endblock body %}

{% block javascripts %}
{% endblock javascripts %}
